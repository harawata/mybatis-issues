<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test.Mapper">

  <resultMap type="test.Employee" id="EmployeeMap">
    <id property="emplId" column="emplId" />
    <collection property="addressList.addresses"
      javaType="list" ofType="test.Address">
      <result column="addressLineOne" property="addressLineOne" />
      <result column="addressLineTwo" property="addressLineTwo" />
      <result column="city" property="city" />
    </collection>
  </resultMap>

  <select id="getEmployee" resultMap="EmployeeMap">
    select * from employee e
    left join address a on a.emplId = e.emplId
    where e.emplId = #{id}
  </select>

</mapper>
